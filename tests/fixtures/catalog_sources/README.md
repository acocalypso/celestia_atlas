# Catalogue importer fixtures

These are small, hand-authored representative rows that exercise the published
VizieR table schemas. They are not bulk catalogue extracts and intentionally
cover only parsing, coordinate, missing-field, note-joining, and failure cases.

The canonical source tables used for an optional local validation build live
under the ignored `.cache/catalog-sources/` directory after the user explicitly
acknowledges the source terms.
